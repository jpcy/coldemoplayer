<Page x:Class="CDP.View.Analysis"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Analysis">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Margin="8" Content="{Binding View}" />
        <Button Grid.Row="1" HorizontalAlignment="Center" Command="{Binding BackCommand}">Back</Button>
    </Grid>
</Page>
