<UserControl x:Class="CDP.View.Error"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Width="300" Height="300" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Text="{Binding Path=Text, Mode=OneWay}" />
        <Button Grid.Row="1" HorizontalAlignment="Center" Command="{Binding OkCommand}">OK</Button>        
    </Grid>
</UserControl>
