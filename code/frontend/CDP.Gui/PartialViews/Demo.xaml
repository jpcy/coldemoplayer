<UserControl x:Class="CDP.Gui.PartialViews.Demo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:l="clr-namespace:CDP.Gui"
    xmlns:uc="clr-namespace:CDP.Gui.UserControls"
    Initialized="UserControl_Initialized">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                        <Image Source="/CDP.Gui;component/Images/details.png" Margin="0,0,4,0" />
                        <TextBlock Text="{x:Static l:Strings.Demo_DetailsTabItemText}" />
                    </StackPanel>
                </TabItem.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Margin="4" VerticalAlignment="Top" Source="{Binding MapThumbnailFileName}" />
                    <ItemsControl Grid.Column="1" Margin="4" Grid.IsSharedSizeScope="True" ItemsSource="{Binding Data.Details}">
                        <ItemsControl.Template>
                            <ControlTemplate>
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </ControlTemplate>
                        </ItemsControl.Template>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="NameColumn" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" FontWeight="Bold" Margin="0,0,10,0" Text="{Binding Name}" FontSize="14" />
                                    <TextBox Grid.Column="1" Margin="0,0,10,0" Style="{StaticResource ReadOnlyNoStyleTextBox}" FontSize="14" Text="{Binding Value}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                        <Image Source="/CDP.Gui;component/Images/players.png" Margin="0,0,4,0" />
                        <TextBlock Text="{x:Static l:Strings.Demo_PlayersTabItemText}" />
                    </StackPanel>
                </TabItem.Header>
                <ListView ItemsSource="{Binding Data.Players}">
                    <ListView.View>
                        <GridView x:Name="playersGridView" />
                    </ListView.View>
                </ListView>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                        <Image Source="/CDP.Gui;component/Images/settings.png" Margin="0,0,4,0" />
                        <TextBlock Text="{x:Static l:Strings.Demo_SettingsTabItemText}" />
                    </StackPanel>
                </TabItem.Header>
                <ContentControl Content="{Binding SettingsView}" />
            </TabItem>
        </TabControl>
        <StackPanel Grid.Row="1" Margin="0,10,0,0" Orientation="Horizontal">
            <l:IconButton Margin="0,0,10,0" Command="{Binding PlayCommand}" IconSource="/Images/play.png" Text="{x:Static l:Strings.PlayButtonText}" Style="{StaticResource ActionIconButton}" />
            <l:IconButton Command="{Binding AnalyseCommand}" IconSource="/Images/analyse.png" Text="{x:Static l:Strings.AnalyseButtonText}" Style="{StaticResource ActionIconButton}" />
        </StackPanel>
    </Grid>
</UserControl>
