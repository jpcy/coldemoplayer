<Page x:Class="CDP.Gui.View.Error"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:view="clr-namespace:CDP.Gui.View"
    xmlns:uc="clr-namespace:CDP.Gui.UserControls"
    Title="Error">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,16,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0,0,0,16" Orientation="Horizontal" HorizontalAlignment="Center">
                <Image Source="/Images/error.png" Width="48" Height="48" />
                <TextBox Margin="0,0,0,4" VerticalAlignment="Center" Text="{Binding Message, Mode=OneWay}" FontSize="18" Style="{StaticResource ReadOnlyNoStyleTextBox}" />
            </StackPanel>
            <TextBox Grid.Row="1" Margin="8" Style="{StaticResource MessageTextBox}" Text="{Binding Exception, Mode=OneWay}" />
        </Grid>
        <uc:Footer Grid.Row="1">
            <uc:IconButton HorizontalAlignment="Center" Margin="0,8,0,8" Command="{Binding OkCommand}" IconSource="/Images/ok.png">OK</uc:IconButton>
        </uc:Footer>
    </Grid>
</Page>
