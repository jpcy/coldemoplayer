<UserControl x:Class="CDP.HalfLife.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CDP.HalfLife">
    <StackPanel Margin="8">
        <GroupBox Header="{x:Static local:Strings.PlaybackMethodGroupBoxHeader}">
            <StackPanel>
                <RadioButton IsChecked="{Binding Path=Playdemo, Mode=TwoWay}">playdemo</RadioButton>
                <RadioButton IsChecked="{Binding Path=Viewdemo, Mode=TwoWay}">viewdemo</RadioButton>
            </StackPanel>
        </GroupBox>
        <CheckBox IsChecked="{Binding StartListenServer}" Content="{x:Static local:Strings.StartListenServerCheckBoxContent}" />
        <CheckBox IsChecked="{Binding RemoveShowscores}" Content="{x:Static local:Strings.RemoveShowscoresCheckBoxContent}" />
    </StackPanel>
</UserControl>
