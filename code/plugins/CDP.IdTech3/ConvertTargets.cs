using System;

namespace CDP.IdTech3
{
    public enum ConvertTargets
    {
        None,
        Protocol68,
        Protocol73 // Possibly temporary, depends on the feasability of converting to QL.
    }
}